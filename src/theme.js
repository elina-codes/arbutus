import { createMuiTheme } from "@material-ui/core/styles"

const arbutusTheme = {
  palette: {
    primary: {
      main: "#01446f",
    },
    secondary: {
      main: "#ff5722",
    },
  },
}

export default createMuiTheme(arbutusTheme)
